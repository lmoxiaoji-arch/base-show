# TechSun WebGL 视差引擎 - 修复与升级全记录 (V5.6)

## 1. 自动化探测与模式识别 (Automation)
*   **全自动版本发现 (Auto-Probe)**：
    *   放弃了硬编码，实现了并行地毯式探测。只要文件夹包含 `top.png` 或 `under.png`，系统会自动收录并生成 UI。
    *   **模式智能判定**：
        *   **SCENE 模式**：通过文件夹后缀 `-c` 自动识别。开启环境云膜，限制倾斜力度（8.0）以保证画面稳重。
        *   **FREE 模式**：无后缀版本。清空背景干扰，释放最高倾斜力度（18.0），适合材质细节全方位展示。
*   **资产共享策略 (Asset Sharing)**：
    *   **云膜共享**：支持根目录 `cloud-12.png` 共享，各版本无需重复存放资源，大幅压缩了项目体积。
    *   **探测标志**：支持 `use_cloud.png` 信号弹文件，手动强制激活特定版本的场景模式。

## 2. 深度空间联动物理 (Parallax Physics)
*   **动态景深效应 (Dynamic DOF)**：
    *   **模糊与倾斜挂钩**：实现了云膜模糊度随“倾斜强度 (Tilt Intensity)”实时变化的逻辑。静止时清晰，大幅侧向倾斜时自动变糊，模拟真实的摄影机对焦感。
*   **三维一体化联动**：
    *   **位移联动**：云膜平移距离 `(x, y)` 随 **阵列尺寸 (Array Size)** 动态缩放。尺寸越大，平移越夸张，空间感越深邃。
    *   **缩放联动**：云膜大小随 **阵列尺寸** 自动匹配，确保在不同景深参数下背景始终覆盖全屏。
*   **物理层级对齐 (Z-Index)**：
    *   标准层级：`Stage背景` -> `Clouds(1) [云膜]` -> `Under(2) [底图]` -> `WebGL(3)` -> `Subject(10) [主体]`。

## 3. UI/UX 工业感重塑 (Restoration)
*   **UI 净值化处理**：
    *   实现了自动化的按钮文本过滤，移除 `v1-c` 等文件夹后缀，界面仅显示纯净的 `V1`, `V2` 等工业标识。
    *   **统一渲染管线**：合并了侧边栏与 HUD 的渲染逻辑，杜绝了多处渲染导致的状态不一致。
*   **交互细节**：
    *   **逻辑唯一化**：移除了多余的 `SCENE/FREE` 标签，回归最纯净的 TechSun 工业视觉准则。
    *   **缓存规避**：在引用脚本中加入版本号 `?v=5.6`，确保用户始终看到最新的逻辑迭代。

## 4. 故障排查与稳态维护 (Stability)
*   **僵尸窗口清理**：编写并留存了 `scratch/kill_window.ps1` 脚本，用于精准清除因自动化工具残留导致的“无法关闭的僵尸窗口”。
*   **3D 渲染冲突修复**：解决了静态 `translateZ` 与 JS 动态旋转之间的冲突，找回了流畅的拖拽感。

---
**当前状态**：项目已实现“素材驱动”的完全自动化。开发者只需增减文件夹并遵循命名规范，引擎即可自动适配所有渲染参数与 UI 交互，达到了工业级的易用性与视觉高度。
